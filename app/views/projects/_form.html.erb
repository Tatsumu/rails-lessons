<%= form_for @project do |f| %>

<p>
	<%= f.label :title %><br>
	<%= f.text_field :title %>
	<% if @project.errors.any? %>
	<%= @project.errors.messages[:title][0] %><!--デフォルトで生成されるエラーメッセージを表示させる -->
	<% end %>
</p>
<p>
	<%= f.submit %>
</p>
<% end %>